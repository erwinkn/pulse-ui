---
title: Object
---

JavaScript Object namespace - static methods for object manipulation.

Note: Object is primarily used as a namespace for static methods.
The types here are as precise as JavaScript's dynamic nature allows.

## Functions

<PyFunction name={"assign"} type={"(target, *sources) -> T"}>

Copy properties from sources to target. Returns target.

<PySourceCode >

```python
@staticmethod
def assign(target: T, *sources: _Any) -> T:
	"""Copy properties from sources to target. Returns target."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"target"} type={"T"} value={null} />
<PyParameter name={"sources"} type={"_Any"} value={"()"} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"create"} type={"(proto, propertiesObject=None) -> _Any"}>

Create a new object with the specified prototype.

<PySourceCode >

```python
@staticmethod
def create(
	proto: _Any | None,
	propertiesObject: dict[str, PropertyDescriptor] | None = None,
	/,
) -> _Any:
	"""Create a new object with the specified prototype."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"proto"} type={"_Any | None"} value={null} />
<PyParameter name={"propertiesObject"} type={"dict[str, PropertyDescriptor] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>

<PyFunction name={"defineProperty"} type={"(obj, prop, descriptor) -> T"}>

Define a property on an object. Returns the object.

<PySourceCode >

```python
@staticmethod
def defineProperty(
	obj: T, prop: str, descriptor: PropertyDescriptor | dict[str, _Any]
) -> T:
	"""Define a property on an object. Returns the object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"T"} value={null} />
<PyParameter name={"prop"} type={"str"} value={null} />
<PyParameter name={"descriptor"} type={"PropertyDescriptor | dict[str, _Any]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"defineProperties"} type={"(obj, props) -> T"}>

Define multiple properties on an object. Returns the object.

<PySourceCode >

```python
@staticmethod
def defineProperties(
	obj: T, props: dict[str, PropertyDescriptor | dict[str, _Any]]
) -> T:
	"""Define multiple properties on an object. Returns the object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"T"} value={null} />
<PyParameter name={"props"} type={"dict[str, PropertyDescriptor | dict[str, _Any]]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"entries"} type={"(obj) -> list[tuple[str, T]]"}>

Return an array of [key, value] pairs.

<PySourceCode >

```python
@staticmethod
def entries(obj: dict[str, T]) -> list[tuple[str, T]]:
	"""Return an array of [key, value] pairs."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"dict[str, T]"} value={null} />

</div>

<PyFunctionReturn type={"list[tuple[str, pulse.js.object.T]]"} />

</PyFunction>

<PyFunction name={"freeze"} type={"(obj) -> T"}>

Freeze an object (prevent modifications). Returns the object.

<PySourceCode >

```python
@staticmethod
def freeze(obj: T) -> T:
	"""Freeze an object (prevent modifications). Returns the object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"fromEntries"} type={"(entries) -> dict[str, T]"}>

Create an object from an iterable of [key, value] pairs.

<PySourceCode >

```python
@staticmethod
def fromEntries(entries: _Iterable[tuple[str, T]]) -> dict[str, T]:
	"""Create an object from an iterable of [key, value] pairs."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"entries"} type={"_Iterable[tuple[str, T]]"} value={null} />

</div>

<PyFunctionReturn type={"dict[str, pulse.js.object.T]"} />

</PyFunction>

<PyFunction name={"getOwnPropertyDescriptor"} type={"(obj, prop) -> PropertyDescriptor | None"}>

Return the property descriptor for a property.

<PySourceCode >

```python
@staticmethod
def getOwnPropertyDescriptor(obj: _Any, prop: str) -> PropertyDescriptor | None:
	"""Return the property descriptor for a property."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />
<PyParameter name={"prop"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.PropertyDescriptor | None"} />

</PyFunction>

<PyFunction name={"getOwnPropertyDescriptors"} type={"(obj) -> dict[str, PropertyDescriptor]"}>

Return all own property descriptors.

<PySourceCode >

```python
@staticmethod
def getOwnPropertyDescriptors(obj: _Any) -> dict[str, PropertyDescriptor]:
	"""Return all own property descriptors."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"dict[str, pulse.js.object.PropertyDescriptor]"} />

</PyFunction>

<PyFunction name={"getOwnPropertyNames"} type={"(obj) -> list[str]"}>

Return all own property names (including non-enumerable).

<PySourceCode >

```python
@staticmethod
def getOwnPropertyNames(obj: _Any) -> list[str]:
	"""Return all own property names (including non-enumerable)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"list[str]"} />

</PyFunction>

<PyFunction name={"getOwnPropertySymbols"} type={"(obj) -> list[_Any]"}>

Return all own Symbol properties.

<PySourceCode >

```python
@staticmethod
def getOwnPropertySymbols(obj: _Any) -> list[_Any]:
	"""Return all own Symbol properties."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"list[typing.Any]"} />

</PyFunction>

<PyFunction name={"getPrototypeOf"} type={"(obj) -> _Any | None"}>

Return the prototype of an object.

<PySourceCode >

```python
@staticmethod
def getPrototypeOf(obj: _Any) -> _Any | None:
	"""Return the prototype of an object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"typing.Any | None"} />

</PyFunction>

<PyFunction name={"hasOwn"} type={"(obj, prop) -> bool"}>

Return True if the object has the specified own property.

<PySourceCode >

```python
@staticmethod
def hasOwn(obj: _Any, prop: str) -> bool:
	"""Return True if the object has the specified own property."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />
<PyParameter name={"prop"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"is_"} type={"(value1, value2) -> bool"}>

Determine if two values are the same value (SameValue algorithm).

<PySourceCode >

```python
@staticmethod
def is_(value1: _Any, value2: _Any) -> bool:
	"""Determine if two values are the same value (SameValue algorithm)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"value1"} type={"_Any"} value={null} />
<PyParameter name={"value2"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"isExtensible"} type={"(obj) -> bool"}>

Return True if the object is extensible.

<PySourceCode >

```python
@staticmethod
def isExtensible(obj: _Any) -> bool:
	"""Return True if the object is extensible."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"isFrozen"} type={"(obj) -> bool"}>

Return True if the object is frozen.

<PySourceCode >

```python
@staticmethod
def isFrozen(obj: _Any) -> bool:
	"""Return True if the object is frozen."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"isSealed"} type={"(obj) -> bool"}>

Return True if the object is sealed.

<PySourceCode >

```python
@staticmethod
def isSealed(obj: _Any) -> bool:
	"""Return True if the object is sealed."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"keys"} type={"(obj) -> list[str]"}>

Return an array of enumerable property names.

<PySourceCode >

```python
@staticmethod
def keys(obj: dict[str, _Any]) -> list[str]:
	"""Return an array of enumerable property names."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"dict[str, _Any]"} value={null} />

</div>

<PyFunctionReturn type={"list[str]"} />

</PyFunction>

<PyFunction name={"preventExtensions"} type={"(obj) -> T"}>

Prevent new properties from being added. Returns the object.

<PySourceCode >

```python
@staticmethod
def preventExtensions(obj: T) -> T:
	"""Prevent new properties from being added. Returns the object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"seal"} type={"(obj) -> T"}>

Seal an object (prevent adding/removing properties). Returns the object.

<PySourceCode >

```python
@staticmethod
def seal(obj: T) -> T:
	"""Seal an object (prevent adding/removing properties). Returns the object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"setPrototypeOf"} type={"(obj, prototype) -> T"}>

Set the prototype of an object. Returns the object.

<PySourceCode >

```python
@staticmethod
def setPrototypeOf(obj: T, prototype: _Any | None) -> T:
	"""Set the prototype of an object. Returns the object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"T"} value={null} />
<PyParameter name={"prototype"} type={"_Any | None"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.object.T"} />

</PyFunction>

<PyFunction name={"values"} type={"(obj) -> list[T]"}>

Return an array of enumerable property values.

<PySourceCode >

```python
@staticmethod
def values(obj: dict[str, T]) -> list[T]:
	"""Return an array of enumerable property values."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"dict[str, T]"} value={null} />

</div>

<PyFunctionReturn type={"list[pulse.js.object.T]"} />

</PyFunction>

<PyFunction name={"groupBy"} type={"(items, keyFn) -> dict[str, list[T]]"}>

Group items by key function result (ES2024).

<PySourceCode >

```python
@staticmethod
def groupBy(items: _Iterable[T], keyFn: _Any) -> dict[str, list[T]]:
	"""Group items by key function result (ES2024)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"items"} type={"_Iterable[T]"} value={null} />
<PyParameter name={"keyFn"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"dict[str, list[pulse.js.object.T]]"} />

</PyFunction>