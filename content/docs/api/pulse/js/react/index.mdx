---
title: react
---

JavaScript React module.

Usage:

```python
from pulse.js.react import useState, useEffect, useRef
state, setState = useState(0)         # -> const [state, setState] = useState(0)
useEffect(lambda: print("hi"), [])    # -> useEffect(() => console.log("hi"), [])
ref = useRef(None)                    # -> const ref = useRef(null)

# Also available as namespace:
import pulse.js.react as React
React.useState(0)                     # -> React.useState(0)
```

<PyAttribute name={"T"} type={null} value={"_TypeVar('T')"} />

<PyAttribute name={"T_co"} type={null} value={"_TypeVar('T_co', covariant=True)"} />

<PyAttribute name={"T_contra"} type={null} value={"_TypeVar('T_contra', contravariant=True)"} />

<PyAttribute name={"S"} type={null} value={"_TypeVar('S')"} />

<PyAttribute name={"A"} type={null} value={"_TypeVar('A')"} />

<PyAttribute name={"lazy"} type={"_LazyComponentFactory"} value={"_LazyComponentFactory()"} />

<Tabs items={["Class","Functions"]}>

<Tab value={"Class"}>

<Cards >

<Card title={"RefObject"} href={"/docs/api/pulse/js/react/RefObject"} />
<Card title={"MutableRefObject"} href={"/docs/api/pulse/js/react/MutableRefObject"} />
<Card title={"Dispatch"} href={"/docs/api/pulse/js/react/Dispatch"} />
<Card title={"TransitionStartFunction"} href={"/docs/api/pulse/js/react/TransitionStartFunction"} />
<Card title={"Context"} href={"/docs/api/pulse/js/react/Context"} />
<Card title={"ReactNode"} href={"/docs/api/pulse/js/react/ReactNode"} />
<Card title={"ReactElement"} href={"/docs/api/pulse/js/react/ReactElement"} />
<Card title={"_LazyComponentFactory"} href={"/docs/api/pulse/js/react/_LazyComponentFactory"} />

</Cards>

</Tab>
<Tab value={"Functions"}>

<PyFunction name={"useState"} type={"(initial_state) -> tuple[S, Dispatch[S | _Callable[[S], S]]]"}>

Returns a stateful value and a function to update it.

Example:

```python
count, set_count = useState(0)
set_count(count + 1)
set_count(lambda prev: prev + 1)
```

<PySourceCode >

```python
def useState(
	initial_state: S | _Callable[[], S],
) -> tuple[S, Dispatch[S | _Callable[[S], S]]]:
	"""Returns a stateful value and a function to update it.

	Example:

	\```python
	count, set_count = useState(0)
	set_count(count + 1)
	set_count(lambda prev: prev + 1)
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"initial_state"} type={"S | _Callable[[], S]"} value={null} />

</div>

<PyFunctionReturn type={"tuple[pulse.js.react.S, pulse.js.react.Dispatch[pulse.js.react.S | collections.abc.Callable[[pulse.js.react.S], pulse.js.react.S]]]"} />

</PyFunction>
<PyFunction name={"useReducer"} type={"(reducer, initial_arg, init=None) -> tuple[S, Dispatch[A]]"}>

An alternative to `useState` for complex state logic.

Example:

```python
def reducer(state, action):
    if action['type'] == 'increment':
        return \{'count': state['count'] + 1\}
    return state

state, dispatch = useReducer(reducer, \{'count': 0\})
dispatch(\{'type': 'increment'\})
```

<PySourceCode >

```python
def useReducer(
	reducer: _Callable[[S, A], S],
	initial_arg: S,
	init: _Callable[[S], S] | None = None,
) -> tuple[S, Dispatch[A]]:
	"""An alternative to `useState` for complex state logic.

	Example:

	\```python
	def reducer(state, action):
	    if action['type'] == 'increment':
	        return {'count': state['count'] + 1}
	    return state

	state, dispatch = useReducer(reducer, {'count': 0})
	dispatch({'type': 'increment'})
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"reducer"} type={"_Callable[[S, A], S]"} value={null} />
<PyParameter name={"initial_arg"} type={"S"} value={null} />
<PyParameter name={"init"} type={"_Callable[[S], S] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"tuple[pulse.js.react.S, pulse.js.react.Dispatch[pulse.js.react.A]]"} />

</PyFunction>
<PyFunction name={"useEffect"} type={"(effect, deps=None) -> None"}>

Accepts a function that contains imperative, possibly effectful code.

Example:

```python
useEffect(lambda: print("mounted"), [])
useEffect(lambda: (print("update"), lambda: print("cleanup"))[-1], [dep])
```

<PySourceCode >

```python
def useEffect(
	effect: _Callable[[], None | _Callable[[], None]],
	deps: list[_Any] | None = None,
) -> None:
	"""Accepts a function that contains imperative, possibly effectful code.

	Example:

	\```python
	useEffect(lambda: print("mounted"), [])
	useEffect(lambda: (print("update"), lambda: print("cleanup"))[-1], [dep])
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"effect"} type={"_Callable[[], None | _Callable[[], None]]"} value={null} />
<PyParameter name={"deps"} type={"list[_Any] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"useLayoutEffect"} type={"(effect, deps=None) -> None"}>

Like `useEffect`, but fires synchronously after all DOM mutations.

Example:

```python
useLayoutEffect(lambda: measure_element(), [])
```

<PySourceCode >

```python
def useLayoutEffect(
	effect: _Callable[[], None | _Callable[[], None]],
	deps: list[_Any] | None = None,
) -> None:
	"""Like `useEffect`, but fires synchronously after all DOM mutations.

	Example:

	\```python
	useLayoutEffect(lambda: measure_element(), [])
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"effect"} type={"_Callable[[], None | _Callable[[], None]]"} value={null} />
<PyParameter name={"deps"} type={"list[_Any] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"useInsertionEffect"} type={"(effect, deps=None) -> None"}>

Like useLayoutEffect, but fires before any DOM mutations.
Use for CSS-in-JS libraries.

<PySourceCode >

```python
def useInsertionEffect(
	effect: _Callable[[], None | _Callable[[], None]],
	deps: list[_Any] | None = None,
) -> None:
	"""Like useLayoutEffect, but fires before any DOM mutations.
	Use for CSS-in-JS libraries.
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"effect"} type={"_Callable[[], None | _Callable[[], None]]"} value={null} />
<PyParameter name={"deps"} type={"list[_Any] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"useRef"} type={"(initial_value) -> MutableRefObject[T]"}>

Returns a mutable ref object.

Example:

```python
input_ref = useRef(None)
# In JSX: \<input ref=\{input_ref\} />
input_ref.current.focus()
```

<PySourceCode >

```python
def useRef(initial_value: T) -> MutableRefObject[T]:
	"""Returns a mutable ref object.

	Example:

	\```python
	input_ref = useRef(None)
	# In JSX: <input ref={input_ref} />
	input_ref.current.focus()
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"initial_value"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.react.MutableRefObject[pulse.js.react.T]"} />

</PyFunction>
<PyFunction name={"useImperativeHandle"} type={"(ref, create_handle, deps=None) -> None"}>

Customizes the instance value exposed to parent components when using ref.

<PySourceCode >

```python
def useImperativeHandle(
	ref: RefObject[T] | _Callable[[T | None], None] | None,
	create_handle: _Callable[[], T],
	deps: list[_Any] | None = None,
) -> None:
	"""Customizes the instance value exposed to parent components when using ref."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"ref"} type={"RefObject[T] | _Callable[[T | None], None] | None"} value={null} />
<PyParameter name={"create_handle"} type={"_Callable[[], T]"} value={null} />
<PyParameter name={"deps"} type={"list[_Any] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"useMemo"} type={"(factory, deps) -> T"}>

Returns a memoized value.

Example:

```python
expensive = useMemo(lambda: compute_expensive(a, b), [a, b])
```

<PySourceCode >

```python
def useMemo(factory: _Callable[[], T], deps: list[_Any]) -> T:
	"""Returns a memoized value.

	Example:

	\```python
	expensive = useMemo(lambda: compute_expensive(a, b), [a, b])
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"factory"} type={"_Callable[[], T]"} value={null} />
<PyParameter name={"deps"} type={"list[_Any]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.react.T"} />

</PyFunction>
<PyFunction name={"useCallback"} type={"(callback, deps) -> T"}>

Returns a memoized callback.

Example:

```python
handle_click = useCallback(lambda e: print(e), [])
```

<PySourceCode >

```python
def useCallback(callback: T, deps: list[_Any]) -> T:
	"""Returns a memoized callback.

	Example:

	\```python
	handle_click = useCallback(lambda e: print(e), [])
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"callback"} type={"T"} value={null} />
<PyParameter name={"deps"} type={"list[_Any]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.react.T"} />

</PyFunction>
<PyFunction name={"useDeferredValue"} type={"(value) -> T"}>

Defers updating a part of the UI. Returns a deferred version of the value.

<PySourceCode >

```python
def useDeferredValue(value: T) -> T:
	"""Defers updating a part of the UI. Returns a deferred version of the value."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"value"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.react.T"} />

</PyFunction>
<PyFunction name={"useTransition"} type={"() -> tuple[bool, TransitionStartFunction]"}>

Returns a stateful value for pending state and a function to start transition.

Example:

```python
is_pending, start_transition = useTransition()
start_transition(lambda: set_state(new_value))
```

<PySourceCode >

```python
def useTransition() -> tuple[bool, TransitionStartFunction]:
	"""Returns a stateful value for pending state and a function to start transition.

	Example:

	\```python
	is_pending, start_transition = useTransition()
	start_transition(lambda: set_state(new_value))
	\```
	"""
	...
```

</PySourceCode>

<PyFunctionReturn type={"tuple[bool, pulse.js.react.TransitionStartFunction]"} />

</PyFunction>
<PyFunction name={"useContext"} type={"(context) -> T"}>

Returns the current context value for the given context.

Example:

```python
theme = useContext(ThemeContext)
```

<PySourceCode >

```python
def useContext(context: Context[T]) -> T:
	"""Returns the current context value for the given context.

	Example:

	\```python
	theme = useContext(ThemeContext)
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"context"} type={"Context[T]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.react.T"} />

</PyFunction>
<PyFunction name={"useId"} type={"() -> str"}>

Generates a unique ID that is stable across server and client.

Example:

```python
id = useId()
# \<label htmlFor=\{id\}>Name\</label>
# \<input id=\{id\} />
```

<PySourceCode >

```python
def useId() -> str:
	"""Generates a unique ID that is stable across server and client.

	Example:

	\```python
	id = useId()
	# <label htmlFor={id}>Name</label>
	# <input id={id} />
	\```
	"""
	...
```

</PySourceCode>

<PyFunctionReturn type={"str"} />

</PyFunction>
<PyFunction name={"useDebugValue"} type={"(value, format_fn=None) -> None"}>

Displays a label in React DevTools for custom hooks.

<PySourceCode >

```python
def useDebugValue(value: T, format_fn: _Callable[[T], _Any] | None = None) -> None:
	"""Displays a label in React DevTools for custom hooks."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"value"} type={"T"} value={null} />
<PyParameter name={"format_fn"} type={"_Callable[[T], _Any] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"useSyncExternalStore"} type={"(subscribe, get_snapshot, get_server_snapshot=None) -> T"}>

Subscribe to an external store.

Example:

```python
width = useSyncExternalStore(
    subscribe_to_resize,
    lambda: window.innerWidth
)
```

<PySourceCode >

```python
def useSyncExternalStore(
	subscribe: _Callable[[_Callable[[], None]], _Callable[[], None]],
	get_snapshot: _Callable[[], T],
	get_server_snapshot: _Callable[[], T] | None = None,
) -> T:
	"""Subscribe to an external store.

	Example:

	\```python
	width = useSyncExternalStore(
	    subscribe_to_resize,
	    lambda: window.innerWidth
	)
	\```
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"subscribe"} type={"_Callable[[_Callable[[], None]], _Callable[[], None]]"} value={null} />
<PyParameter name={"get_snapshot"} type={"_Callable[[], T]"} value={null} />
<PyParameter name={"get_server_snapshot"} type={"_Callable[[], T] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.react.T"} />

</PyFunction>
<PyFunction name={"createElement"} type={"(type, props=None, *children) -> ReactElement"}>

Creates a React element.

<PySourceCode >

```python
def createElement(
	type: _Any,
	props: dict[str, _Any] | None = None,
	*children: _Any,
) -> ReactElement:
	"""Creates a React element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"type"} type={"_Any"} value={null} />
<PyParameter name={"props"} type={"dict[str, _Any] | None"} value={"None"} />
<PyParameter name={"children"} type={"_Any"} value={"()"} />

</div>

<PyFunctionReturn type={"pulse.js.react.ReactElement"} />

</PyFunction>
<PyFunction name={"cloneElement"} type={"(element, props=None, *children) -> ReactElement"}>

Clones and returns a new React element.

<PySourceCode >

```python
def cloneElement(
	element: ReactElement,
	props: dict[str, _Any] | None = None,
	*children: _Any,
) -> ReactElement:
	"""Clones and returns a new React element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"element"} type={"ReactElement"} value={null} />
<PyParameter name={"props"} type={"dict[str, _Any] | None"} value={"None"} />
<PyParameter name={"children"} type={"_Any"} value={"()"} />

</div>

<PyFunctionReturn type={"pulse.js.react.ReactElement"} />

</PyFunction>
<PyFunction name={"isValidElement"} type={"(obj) -> bool"}>

Checks if the object is a React element.

<PySourceCode >

```python
def isValidElement(obj: _Any) -> bool:
	"""Checks if the object is a React element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>
<PyFunction name={"memo"} type={"(component, are_equal=None) -> T"}>

Memoizes a component to skip re-rendering when props are unchanged.

<PySourceCode >

```python
def memo(component: T, are_equal: _Callable[[_Any, _Any], bool] | None = None) -> T:
	"""Memoizes a component to skip re-rendering when props are unchanged."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"component"} type={"T"} value={null} />
<PyParameter name={"are_equal"} type={"_Callable[[_Any, _Any], bool] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.react.T"} />

</PyFunction>
<PyFunction name={"forwardRef"} type={"(render) -> _Callable[..., ReactElement | None]"}>

Lets your component expose a DOM node to a parent component with a ref.

<PySourceCode >

```python
def forwardRef(
	render: _Callable[[_Any, _Any], ReactElement | None],
) -> _Callable[..., ReactElement | None]:
	"""Lets your component expose a DOM node to a parent component with a ref."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"render"} type={"_Callable[[_Any, _Any], ReactElement | None]"} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Callable[..., pulse.js.react.ReactElement | None]"} />

</PyFunction>
<PyFunction name={"createContext"} type={"(default_value) -> Context[T]"}>

Creates a Context object.

<PySourceCode >

```python
def createContext(default_value: T) -> Context[T]:
	"""Creates a Context object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"default_value"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.react.Context[pulse.js.react.T]"} />

</PyFunction>
<PyFunction name={"Suspense"} type={"(*, fallback=None, name=None) -> ReactElement"}>

Lets you display a fallback while its children are loading.

<PySourceCode >

```python
@_component
def Suspense(
	*, fallback: ReactNode | _PulseNode | None = None, name: str | None = None
) -> ReactElement:
	"""Lets you display a fallback while its children are loading."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"fallback"} type={"ReactNode | _PulseNode | None"} value={"None"} />
<PyParameter name={"name"} type={"str | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.react.ReactElement"} />

</PyFunction>

</Tab>

</Tabs>