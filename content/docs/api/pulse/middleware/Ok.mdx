---
title: Ok
---

Success response wrapper.

Use ``Ok(None)`` for void success, or ``Ok(payload)`` to wrap a value.

## Attributes

<PyAttribute name={"payload"} type={"T"} value={"payload"}>

The wrapped success value.

</PyAttribute>

## Functions

<PyFunction name={"__init__"} type={"(self, payload=None) -> None"}>

Initialize a success response.

<PySourceCode >

```python
def __init__(self, payload: T | None = None) -> None:
	"""Initialize a success response.

	Args:
		payload: The success value (optional, defaults to None).
	"""
	self.payload = payload  # pyright: ignore[reportAttributeAccessIssue]
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"payload"} type={"T | None"} value={"None"}>

The success value (optional, defaults to None).

</PyParameter>

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>