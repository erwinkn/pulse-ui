---
title: client
---

<PyAttribute name={"T"} type={null} value={"TypeVar('T')"} />

<PyAttribute name={"QueryFilter"} type={null} value={"QueryKey | list[QueryKey] | Callable[[QueryKey], bool]"} />

<PyAttribute name={"queries"} type={null} value={"QueryClient()"} />

<Tabs items={["Class","Functions"]}>

<Tab value={"Class"}>

<Cards >

<Card title={"QueryClient"} href={"/docs/api/pulse/queries/client/QueryClient"} />

</Cards>

</Tab>
<Tab value={"Functions"}>

<PyFunction name={"_normalize_filter"} type={"(filter) -> Callable[[QueryKey], bool] | None"}>

Convert any QueryFilter to a predicate function.

<PySourceCode >

```python
def _normalize_filter(
	filter: QueryFilter | None,
) -> Callable[[QueryKey], bool] | None:
	"""Convert any QueryFilter to a predicate function."""
	if filter is None:
		return None
	if isinstance(filter, tuple):
		# Exact key match
		exact_key = filter
		return lambda k: k == exact_key
	if isinstance(filter, list):
		# List of keys
		key_set = set(filter)
		return lambda k: k in key_set
	# Already a callable predicate
	return filter
```

</PySourceCode>

<div >

<PyParameter name={"filter"} type={"QueryFilter | None"} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Callable[[pulse.queries.common.QueryKey], bool] | None"} />

</PyFunction>
<PyFunction name={"_prefix_filter"} type={"(prefix) -> Callable[[QueryKey], bool]"}>

Create a predicate that matches keys starting with the given prefix.

<PySourceCode >

```python
def _prefix_filter(prefix: tuple[Any, ...]) -> Callable[[QueryKey], bool]:
	"""Create a predicate that matches keys starting with the given prefix."""
	prefix_len = len(prefix)
	return lambda k: len(k) >= prefix_len and k[:prefix_len] == prefix
```

</PySourceCode>

<div >

<PyParameter name={"prefix"} type={"tuple[Any, ...]"} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Callable[[pulse.queries.common.QueryKey], bool]"} />

</PyFunction>

</Tab>

</Tabs>