---
title: PyJson
---

Provides transpilation for Python json functions to JavaScript.

## Functions

<PyFunction name={"dumps"} type={"(obj, *, ctx) -> Expr"}>

<PySourceCode >

```python
@staticmethod
def dumps(obj: Any, *, ctx: Transpiler) -> Expr:
	return Call(Member(_JSON, "stringify"), [ctx.emit_expr(obj)])
```

</PySourceCode>

<div >

<PyParameter name={"obj"} type={"Any"} value={null} />
<PyParameter name={"ctx"} type={"Transpiler"} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.nodes.Expr"} />

</PyFunction>

<PyFunction name={"loads"} type={"(s, *, ctx) -> Expr"}>

<PySourceCode >

```python
@staticmethod
def loads(s: Any, *, ctx: Transpiler) -> Expr:
	return Call(Member(_JSON, "parse"), [ctx.emit_expr(s)])
```

</PySourceCode>

<div >

<PyParameter name={"s"} type={"Any"} value={null} />
<PyParameter name={"ctx"} type={"Transpiler"} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.nodes.Expr"} />

</PyFunction>