---
title: math
---

Python math module transpilation to JavaScript Math.

Provides transpilation from Python's `math` module to JavaScript's `Math` object.

<Tabs items={["Class","Functions"]}>

<Tab value={"Class"}>

<Cards >

<Card title={"PyMath"} href={"/docs/api/pulse/transpiler/modules/math/PyMath"} />

</Cards>

</Tab>
<Tab value={"Functions"}>

<PyFunction name={"_math_prop"} type={"(name) -> Expr"}>

<PySourceCode >

```python
def _math_prop(name: str) -> Expr:
	return Member(_Math, name)
```

</PySourceCode>

<div >

<PyParameter name={"name"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.nodes.Expr"} />

</PyFunction>
<PyFunction name={"_math_call"} type={"(name, args) -> Expr"}>

<PySourceCode >

```python
def _math_call(name: str, args: list[Expr]) -> Expr:
	return Call(Member(_Math, name), args)
```

</PySourceCode>

<div >

<PyParameter name={"name"} type={"str"} value={null} />
<PyParameter name={"args"} type={"list[Expr]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.nodes.Expr"} />

</PyFunction>
<PyFunction name={"_number_call"} type={"(name, args) -> Expr"}>

<PySourceCode >

```python
def _number_call(name: str, args: list[Expr]) -> Expr:
	return Call(Member(_Number, name), args)
```

</PySourceCode>

<div >

<PyParameter name={"name"} type={"str"} value={null} />
<PyParameter name={"args"} type={"list[Expr]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.nodes.Expr"} />

</PyFunction>

</Tab>

</Tabs>