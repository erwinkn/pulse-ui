---
title: Array
---

JS array: [a, b, c]

## Attributes

<PyAttribute name={"elements"} type={"Sequence[Expr]"} value={null} />

## Functions

<PyFunction name={"emit"} type={"(self, out) -> None"}>

<PySourceCode >

```python
@override
def emit(self, out: list[str]) -> None:
	out.append("[")
	for i, e in enumerate(self.elements):
		if i > 0:
			out.append(", ")
		e.emit(out)
	out.append("]")
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"out"} type={"list[str]"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

<PyFunction name={"render"} type={"(self) -> VDOMNode"}>

<PySourceCode >

```python
@override
def render(self) -> VDOMNode:
	return {"t": "array", "items": [e.render() for e in self.elements]}
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.vdom.VDOMNode"} />

</PyFunction>

<PyFunction name={"__init__"} type={"(self, elements) -> None"}>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"elements"} type={"Sequence[Expr]"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>