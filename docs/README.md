# Pulse Docs

Fumadocs + Next.js app for Pulse docs.

Run dev server:

```bash
bun run dev
```

Key paths:

- `content/docs`: MDX content
- `lib/source.ts`: Fumadocs source loader
- `app/docs`: docs layout + routes
