---
title: "Claude Code"
---

Recommended setup:
- Put `CLAUDE.md` at repo root
- Use `make all` before commit

Prompt template:

```text
Read AGENTS.md. Use uv + bun. Run make all before commit.
```
