---
title: "FilterNoisyRequests"
---

Filter out noisy requests from uvicorn access logs.

## Attributes

<PyAttribute name={"SUPPRESS_PATTERNS"} type={"tuple[str, ...]"} value={"('/node_modules/', '/@vite/', '/%40vite/', '/@fs/', '/%40fs/', '/@id/', '/%40id/', '/@react-refresh', '/app/', '.js?v=', '.css?v=', '.css ', '.tsx ', '.ts ', '.js ', '.map', '/favicon.ico', '/.well-known/', 'connection open', 'connection closed', 'connection rejected', '304 Not Modified')"} />

## Functions

<PyFunction name={"filter"} type={"(self, record) -> bool"}>

Return False to suppress log records matching noise patterns.

<PySourceCode >

```python
@override
def filter(self, record: logging.LogRecord) -> bool:
	"""Return False to suppress log records matching noise patterns."""
	message = record.getMessage()
	# Suppress if message contains any noise pattern
	return not any(pattern in message for pattern in self.SUPPRESS_PATTERNS)
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"record"} type={"logging.LogRecord"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>