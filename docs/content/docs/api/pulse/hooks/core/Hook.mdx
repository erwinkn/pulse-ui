---
title: "Hook"
---

A registered hook definition.

Hooks are created via ``ps.hooks.create()`` and can be called during
component render to access their associated state.

## Attributes

<PyAttribute name={"name"} type={"str"} value={null}>

Unique name identifying this hook.

</PyAttribute>

<PyAttribute name={"factory"} type={"HookFactory[T]"} value={null}>

Function that creates new HookState instances.

</PyAttribute>

<PyAttribute name={"metadata"} type={"HookMetadata"} value={null}>

Optional metadata about the hook.

</PyAttribute>

## Functions

<PyFunction name={"__call__"} type={"(self, key=None) -> T"}>

Get or create hook state for the current component.

<PySourceCode >

```python
def __call__(self, key: str | None = None) -> T:
	"""Get or create hook state for the current component.

	Args:
		key: Optional key for multiple instances of the same hook.

	Returns:
		The hook state instance.

	Raises:
		HookError: If called outside of a render context.
	"""
	ctx = HookContext.require(self.name)
	namespace = ctx.namespace_for(self)
	state = namespace.ensure(ctx, key)
	return state
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"key"} type={"str | None"} value={"None"}>

Optional key for multiple instances of the same hook.

</PyParameter>

</div>

<PyFunctionReturn type={"pulse.hooks.core.T"}>

The hook state instance.

</PyFunctionReturn>

</PyFunction>

<PyFunction name={"__init__"} type={"(self, name, factory, metadata) -> None"}>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"name"} type={"str"} value={null} />
<PyParameter name={"factory"} type={"HookFactory[T]"} value={null} />
<PyParameter name={"metadata"} type={"HookMetadata"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>