---
title: "Array"
---

JavaScript Array - a generic indexed collection.

Array[T] represents a JavaScript array containing elements of type T.
All instance methods preserve the expected generic types.

## Attributes

<PyAttribute name={"length"} type={"int"} value={null}>

The number of elements in the array.

</PyAttribute>

## Functions

<PyFunction name={"__init__"} type={"(self, *args) -> None"}>

Create an Array.

- No arguments: empty array
- Single int: array with that length
- Multiple args: array with those elements

<PySourceCode >

```python
def __init__(self, *args: T | int) -> None:
	"""Create an Array.

	- No arguments: empty array
	- Single int: array with that length
	- Multiple args: array with those elements
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"args"} type={"T | int"} value={"()"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

<PyFunction name={"isArray"} type={"(value) -> _TypeGuard['Array[_Any]']"}>

Determine whether the passed value is an Array.

<PySourceCode >

```python
@staticmethod
def isArray(value: _Any, /) -> _TypeGuard["Array[_Any]"]:
	"""Determine whether the passed value is an Array."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"value"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"typing.TypeGuard['Array[_Any]']"} />

</PyFunction>

<PyFunction name={"from_"} type={"(arrayLike, mapFn=None, thisArg=None) -> 'Array[_U] | Array[_T]'"}>

Create a new Array from an array-like or iterable object.

<PySourceCode >

```python
@staticmethod
def from_(
	arrayLike: _Iterable[_T] | "Array[_T]",
	mapFn: _Callable[[_T, int], _U] | None = None,
	thisArg: _Any | None = None,
	/,
) -> "Array[_U] | Array[_T]":
	"""Create a new Array from an array-like or iterable object."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"arrayLike"} type={"_Iterable[_T] | 'Array[_T]'"} value={null} />
<PyParameter name={"mapFn"} type={"_Callable[[_T, int], _U] | None"} value={"None"} />
<PyParameter name={"thisArg"} type={"_Any | None"} value={"None"} />

</div>

<PyFunctionReturn type={"'Array[_U] | Array[_T]'"} />

</PyFunction>

<PyFunction name={"of"} type={"(*elements) -> 'Array[_T]'"}>

Create a new Array from a variable number of arguments.

<PySourceCode >

```python
@staticmethod
def of(*elements: _T) -> "Array[_T]":
	"""Create a new Array from a variable number of arguments."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"elements"} type={"_T"} value={"()"} />

</div>

<PyFunctionReturn type={"'Array[_T]'"} />

</PyFunction>

<PyFunction name={"push"} type={"(self, *items) -> int"}>

Add elements to the end; returns new length.

<PySourceCode >

```python
def push(self, *items: T) -> int:
	"""Add elements to the end; returns new length."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"items"} type={"T"} value={"()"} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"pop"} type={"(self) -> T | None"}>

Remove and return the last element.

<PySourceCode >

```python
def pop(self) -> T | None:
	"""Remove and return the last element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.array.T | None"} />

</PyFunction>

<PyFunction name={"shift"} type={"(self) -> T | None"}>

Remove and return the first element.

<PySourceCode >

```python
def shift(self) -> T | None:
	"""Remove and return the first element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.array.T | None"} />

</PyFunction>

<PyFunction name={"unshift"} type={"(self, *items) -> int"}>

Add elements to the beginning; returns new length.

<PySourceCode >

```python
def unshift(self, *items: T) -> int:
	"""Add elements to the beginning; returns new length."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"items"} type={"T"} value={"()"} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"splice"} type={"(self, start, deleteCount=None, *items) -> 'Array[T]'"}>

Remove/replace elements and optionally insert new ones.

<PySourceCode >

```python
def splice(
	self, start: int, deleteCount: int | None = None, /, *items: T
) -> "Array[T]":
	"""Remove/replace elements and optionally insert new ones."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"start"} type={"int"} value={null} />
<PyParameter name={"deleteCount"} type={"int | None"} value={"None"} />
<PyParameter name={"items"} type={"T"} value={"()"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"reverse"} type={"(self) -> 'Array[T]'"}>

Reverse the array in place.

<PySourceCode >

```python
def reverse(self) -> "Array[T]":
	"""Reverse the array in place."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"sort"} type={"(self, compareFn=None) -> 'Array[T]'"}>

Sort the array in place.

<PySourceCode >

```python
def sort(self, compareFn: _Callable[[T, T], int] | None = None, /) -> "Array[T]":
	"""Sort the array in place."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"compareFn"} type={"_Callable[[T, T], int] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"fill"} type={"(self, value, start=0, end=None) -> 'Array[T]'"}>

Fill all elements with a static value.

<PySourceCode >

```python
def fill(self, value: T, start: int = 0, end: int | None = None, /) -> "Array[T]":
	"""Fill all elements with a static value."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"value"} type={"T"} value={null} />
<PyParameter name={"start"} type={"int"} value={"0"} />
<PyParameter name={"end"} type={"int | None"} value={"None"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"copyWithin"} type={"(self, target, start=0, end=None) -> 'Array[T]'"}>

Copy part of the array to another location within it.

<PySourceCode >

```python
def copyWithin(
	self, target: int, start: int = 0, end: int | None = None, /
) -> "Array[T]":
	"""Copy part of the array to another location within it."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"target"} type={"int"} value={null} />
<PyParameter name={"start"} type={"int"} value={"0"} />
<PyParameter name={"end"} type={"int | None"} value={"None"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"concat"} type={"(self, *items) -> 'Array[T]'"}>

Merge arrays and/or values into a new array.

<PySourceCode >

```python
def concat(self, *items: T | "_Iterable[T]") -> "Array[T]":
	"""Merge arrays and/or values into a new array."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"items"} type={"T | '_Iterable[T]'"} value={"()"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"slice"} type={"(self, start=0, end=None) -> 'Array[T]'"}>

Return a shallow copy of a portion of the array.

<PySourceCode >

```python
def slice(self, start: int = 0, end: int | None = None, /) -> "Array[T]":
	"""Return a shallow copy of a portion of the array."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"start"} type={"int"} value={"0"} />
<PyParameter name={"end"} type={"int | None"} value={"None"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"join"} type={"(self, separator=',') -> str"}>

Join all elements into a string.

<PySourceCode >

```python
def join(self, separator: str = ",", /) -> str:
	"""Join all elements into a string."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"separator"} type={"str"} value={"','"} />

</div>

<PyFunctionReturn type={"str"} />

</PyFunction>

<PyFunction name={"indexOf"} type={"(self, searchElement, fromIndex=0) -> int"}>

Return first index of element, or -1 if not found.

<PySourceCode >

```python
def indexOf(self, searchElement: T, fromIndex: int = 0, /) -> int:
	"""Return first index of element, or -1 if not found."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"searchElement"} type={"T"} value={null} />
<PyParameter name={"fromIndex"} type={"int"} value={"0"} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"lastIndexOf"} type={"(self, searchElement, fromIndex=None) -> int"}>

Return last index of element, or -1 if not found.

<PySourceCode >

```python
def lastIndexOf(self, searchElement: T, fromIndex: int | None = None, /) -> int:
	"""Return last index of element, or -1 if not found."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"searchElement"} type={"T"} value={null} />
<PyParameter name={"fromIndex"} type={"int | None"} value={"None"} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"includes"} type={"(self, searchElement, fromIndex=0) -> bool"}>

Determine whether the array contains the element.

<PySourceCode >

```python
def includes(self, searchElement: T, fromIndex: int = 0, /) -> bool:
	"""Determine whether the array contains the element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"searchElement"} type={"T"} value={null} />
<PyParameter name={"fromIndex"} type={"int"} value={"0"} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"at"} type={"(self, index) -> T | None"}>

Return element at index (supports negative indexing).

<PySourceCode >

```python
def at(self, index: int) -> T | None:
	"""Return element at index (supports negative indexing)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"index"} type={"int"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.array.T | None"} />

</PyFunction>

<PyFunction name={"flat"} type={"(self, depth=1) -> 'Array[_Any]'"}>

Flatten nested arrays to the specified depth.

<PySourceCode >

```python
def flat(self, depth: int = 1, /) -> "Array[_Any]":
	"""Flatten nested arrays to the specified depth."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"depth"} type={"int"} value={"1"} />

</div>

<PyFunctionReturn type={"'Array[_Any]'"} />

</PyFunction>

<PyFunction name={"flatMap"} type={"(self, callback) -> 'Array[U]'"}>

Map then flatten the result by one level.

<PySourceCode >

```python
def flatMap(
	self, callback: _Callable[[T, int, "Array[T]"], _Iterable[U]]
) -> "Array[U]":
	"""Map then flatten the result by one level."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], _Iterable[U]]"} value={null} />

</div>

<PyFunctionReturn type={"'Array[U]'"} />

</PyFunction>

<PyFunction name={"toReversed"} type={"(self) -> 'Array[T]'"}>

Return a new reversed array (ES2023).

<PySourceCode >

```python
def toReversed(self) -> "Array[T]":
	"""Return a new reversed array (ES2023)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"toSorted"} type={"(self, compareFn=None) -> 'Array[T]'"}>

Return a new sorted array (ES2023).

<PySourceCode >

```python
def toSorted(
	self, compareFn: _Callable[[T, T], int] | None = None, /
) -> "Array[T]":
	"""Return a new sorted array (ES2023)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"compareFn"} type={"_Callable[[T, T], int] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"toSpliced"} type={"(self, start, deleteCount=None, *items) -> 'Array[T]'"}>

Return a new array with elements spliced (ES2023).

<PySourceCode >

```python
def toSpliced(
	self, start: int, deleteCount: int | None = None, /, *items: T
) -> "Array[T]":
	"""Return a new array with elements spliced (ES2023)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"start"} type={"int"} value={null} />
<PyParameter name={"deleteCount"} type={"int | None"} value={"None"} />
<PyParameter name={"items"} type={"T"} value={"()"} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"with_"} type={"(self, index, value) -> 'Array[T]'"}>

Return a new array with element at index replaced (ES2023).

<PySourceCode >

```python
def with_(self, index: int, value: T) -> "Array[T]":
	"""Return a new array with element at index replaced (ES2023)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"index"} type={"int"} value={null} />
<PyParameter name={"value"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"forEach"} type={"(self, callback) -> None"}>

Execute a function for each element.

<PySourceCode >

```python
def forEach(self, callback: _Callable[[T, int, "Array[T]"], None]) -> None:
	"""Execute a function for each element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], None]"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

<PyFunction name={"map"} type={"(self, callback) -> 'Array[U]'"}>

Create a new array with results of calling callback on each element.

<PySourceCode >

```python
def map(self, callback: _Callable[[T, int, "Array[T]"], U]) -> "Array[U]":
	"""Create a new array with results of calling callback on each element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], U]"} value={null} />

</div>

<PyFunctionReturn type={"'Array[U]'"} />

</PyFunction>

<PyFunction name={"filter"} type={"(self, callback) -> 'Array[T]'"}>

Create a new array with elements that pass the test.

<PySourceCode >

```python
def filter(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> "Array[T]":
	"""Create a new array with elements that pass the test."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"'Array[T]'"} />

</PyFunction>

<PyFunction name={"reduce"} type={"(self, callback, initialValue=None) -> U"}>

Reduce array to a single value (left to right).

If no initialValue is provided, the first element is used.

<PySourceCode >

```python
def reduce(
	self,
	callback: _Callable[[U, T, int, "Array[T]"], U],
	initialValue: U | None = None,
	/,
) -> U:
	"""Reduce array to a single value (left to right).

	If no initialValue is provided, the first element is used.
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[U, T, int, 'Array[T]'], U]"} value={null} />
<PyParameter name={"initialValue"} type={"U | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.array.U"} />

</PyFunction>

<PyFunction name={"reduceRight"} type={"(self, callback, initialValue=None) -> U"}>

Reduce array to a single value (right to left).

If no initialValue is provided, the last element is used.

<PySourceCode >

```python
def reduceRight(
	self,
	callback: _Callable[[U, T, int, "Array[T]"], U],
	initialValue: U | None = None,
	/,
) -> U:
	"""Reduce array to a single value (right to left).

	If no initialValue is provided, the last element is used.
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[U, T, int, 'Array[T]'], U]"} value={null} />
<PyParameter name={"initialValue"} type={"U | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.array.U"} />

</PyFunction>

<PyFunction name={"find"} type={"(self, callback) -> T | None"}>

Return first element satisfying the callback.

<PySourceCode >

```python
def find(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> T | None:
	"""Return first element satisfying the callback."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.array.T | None"} />

</PyFunction>

<PyFunction name={"findIndex"} type={"(self, callback) -> int"}>

Return index of first element satisfying the callback, or -1.

<PySourceCode >

```python
def findIndex(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> int:
	"""Return index of first element satisfying the callback, or -1."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"findLast"} type={"(self, callback) -> T | None"}>

Return last element satisfying the callback (ES2023).

<PySourceCode >

```python
def findLast(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> T | None:
	"""Return last element satisfying the callback (ES2023)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.array.T | None"} />

</PyFunction>

<PyFunction name={"findLastIndex"} type={"(self, callback) -> int"}>

Return index of last element satisfying the callback, or -1 (ES2023).

<PySourceCode >

```python
def findLastIndex(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> int:
	"""Return index of last element satisfying the callback, or -1 (ES2023)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"every"} type={"(self, callback) -> bool"}>

Test whether all elements pass the callback.

<PySourceCode >

```python
def every(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> bool:
	"""Test whether all elements pass the callback."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"some"} type={"(self, callback) -> bool"}>

Test whether at least one element passes the callback.

<PySourceCode >

```python
def some(self, callback: _Callable[[T, int, "Array[T]"], bool]) -> bool:
	"""Test whether at least one element passes the callback."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"callback"} type={"_Callable[[T, int, 'Array[T]'], bool]"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>

<PyFunction name={"keys"} type={"(self) -> _Iterable[int]"}>

Return an iterator of array indices.

<PySourceCode >

```python
def keys(self) -> _Iterable[int]:
	"""Return an iterator of array indices."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Iterable[int]"} />

</PyFunction>

<PyFunction name={"values"} type={"(self) -> _Iterable[T]"}>

Return an iterator of array values.

<PySourceCode >

```python
def values(self) -> _Iterable[T]:
	"""Return an iterator of array values."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Iterable[pulse.js.array.T]"} />

</PyFunction>

<PyFunction name={"entries"} type={"(self) -> _Iterable[tuple[int, T]]"}>

Return an iterator of [index, value] pairs.

<PySourceCode >

```python
def entries(self) -> _Iterable[tuple[int, T]]:
	"""Return an iterator of [index, value] pairs."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Iterable[tuple[int, pulse.js.array.T]]"} />

</PyFunction>

<PyFunction name={"toString"} type={"(self) -> str"}>

Return a string representing the array.

<PySourceCode >

```python
def toString(self) -> str:
	"""Return a string representing the array."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"str"} />

</PyFunction>

<PyFunction name={"toLocaleString"} type={"(self) -> str"}>

Return a localized string representing the array.

<PySourceCode >

```python
def toLocaleString(self) -> str:
	"""Return a localized string representing the array."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"str"} />

</PyFunction>

<PyFunction name={"__iter__"} type={"(self) -> _Iterator[T]"}>

Iterate over array elements.

<PySourceCode >

```python
def __iter__(self) -> _Iterator[T]:
	"""Iterate over array elements."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Iterator[pulse.js.array.T]"} />

</PyFunction>

<PyFunction name={"__getitem__"} type={"(self, index) -> T"}>

Get element at index.

<PySourceCode >

```python
def __getitem__(self, index: int) -> T:
	"""Get element at index."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"index"} type={"int"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.array.T"} />

</PyFunction>

<PyFunction name={"__setitem__"} type={"(self, index, value) -> None"}>

Set element at index.

<PySourceCode >

```python
def __setitem__(self, index: int, value: T) -> None:
	"""Set element at index."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"index"} type={"int"} value={null} />
<PyParameter name={"value"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

<PyFunction name={"__len__"} type={"(self) -> int"}>

Return the number of elements (same as length).

<PySourceCode >

```python
def __len__(self) -> int:
	"""Return the number of elements (same as length)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"__contains__"} type={"(self, value) -> bool"}>

Check if value exists in the array (same as includes).

<PySourceCode >

```python
def __contains__(self, value: T) -> bool:
	"""Check if value exists in the array (same as includes)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"value"} type={"T"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>