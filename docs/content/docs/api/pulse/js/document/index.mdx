---
title: "document"
---

Browser document global object.

Usage:
    from pulse.js import document
    document.querySelector("#app")      # -> document.querySelector("#app")

<PyAttribute name={"body"} type={"_HTMLElement"} value={null} />

<PyAttribute name={"head"} type={"_HTMLElement"} value={null} />

<PyAttribute name={"documentElement"} type={"_HTMLElement"} value={null} />

<PyAttribute name={"activeElement"} type={"_Element | None"} value={null} />

<PyAttribute name={"title"} type={"str"} value={null} />

<PyAttribute name={"readyState"} type={"str"} value={null} />

<PyAttribute name={"cookie"} type={"str"} value={null} />

<PyAttribute name={"referrer"} type={"str"} value={null} />

<PyAttribute name={"URL"} type={"str"} value={null} />

<PyAttribute name={"domain"} type={"str"} value={null} />

<PyAttribute name={"fullscreenElement"} type={"_Element | None"} value={null} />

<Tabs items={["Functions"]}>

<Tab value={"Functions"}>

<PyFunction name={"querySelector"} type={"(selectors) -> _Element | None"}>

Return the first element matching the selector, or None.

<PySourceCode >

```python
def querySelector(selectors: str) -> _Element | None:
	"""Return the first element matching the selector, or None."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"selectors"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.Element | None"} />

</PyFunction>
<PyFunction name={"querySelectorAll"} type={"(selectors) -> _NodeList[_Element]"}>

Return all elements matching the selector.

<PySourceCode >

```python
def querySelectorAll(selectors: str) -> _NodeList[_Element]:
	"""Return all elements matching the selector."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"selectors"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.NodeList[pulse.js._types.Element]"} />

</PyFunction>
<PyFunction name={"getElementById"} type={"(elementId) -> _Element | None"}>

Return the element with the given ID, or None.

<PySourceCode >

```python
def getElementById(elementId: str) -> _Element | None:
	"""Return the element with the given ID, or None."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"elementId"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.Element | None"} />

</PyFunction>
<PyFunction name={"getElementsByClassName"} type={"(classNames) -> _HTMLCollection[_Element]"}>

Return all elements with the given class name(s).

<PySourceCode >

```python
def getElementsByClassName(classNames: str) -> _HTMLCollection[_Element]:
	"""Return all elements with the given class name(s)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"classNames"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.HTMLCollection[pulse.js._types.Element]"} />

</PyFunction>
<PyFunction name={"getElementsByTagName"} type={"(qualifiedName) -> _HTMLCollection[_Element]"}>

Return all elements with the given tag name.

<PySourceCode >

```python
def getElementsByTagName(qualifiedName: str) -> _HTMLCollection[_Element]:
	"""Return all elements with the given tag name."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"qualifiedName"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.HTMLCollection[pulse.js._types.Element]"} />

</PyFunction>
<PyFunction name={"getElementsByName"} type={"(elementName) -> _NodeList[_Element]"}>

Return all elements with the given name attribute.

<PySourceCode >

```python
def getElementsByName(elementName: str) -> _NodeList[_Element]:
	"""Return all elements with the given name attribute."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"elementName"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.NodeList[pulse.js._types.Element]"} />

</PyFunction>
<PyFunction name={"createElement"} type={"(tagName, options=None) -> _HTMLElement"}>

Create a new element with the given tag name.

<PySourceCode >

```python
def createElement(
	tagName: str, options: dict[str, str] | None = None, /
) -> _HTMLElement:
	"""Create a new element with the given tag name."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"tagName"} type={"str"} value={null} />
<PyParameter name={"options"} type={"dict[str, str] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js._types.HTMLElement"} />

</PyFunction>
<PyFunction name={"createTextNode"} type={"(data) -> _Any"}>

Create a new text node.

<PySourceCode >

```python
def createTextNode(data: str) -> _Any:
	"""Create a new text node."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"data"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>
<PyFunction name={"createDocumentFragment"} type={"() -> _Any"}>

Create a new document fragment.

<PySourceCode >

```python
def createDocumentFragment() -> _Any:
	"""Create a new document fragment."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>
<PyFunction name={"createComment"} type={"(data) -> _Any"}>

Create a new comment node.

<PySourceCode >

```python
def createComment(data: str) -> _Any:
	"""Create a new comment node."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"data"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>
<PyFunction name={"addEventListener"} type={"(type, listener, options=None) -> None"}>

Add an event listener to the document.

<PySourceCode >

```python
def addEventListener(
	type: str,
	listener: _Callable[..., None],
	options: bool | dict[str, bool] | None = None,
	/,
) -> None:
	"""Add an event listener to the document."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"type"} type={"str"} value={null} />
<PyParameter name={"listener"} type={"_Callable[..., None]"} value={null} />
<PyParameter name={"options"} type={"bool | dict[str, bool] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"removeEventListener"} type={"(type, listener, options=None) -> None"}>

Remove an event listener from the document.

<PySourceCode >

```python
def removeEventListener(
	type: str,
	listener: _Callable[..., None],
	options: bool | dict[str, bool] | None = None,
	/,
) -> None:
	"""Remove an event listener from the document."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"type"} type={"str"} value={null} />
<PyParameter name={"listener"} type={"_Callable[..., None]"} value={null} />
<PyParameter name={"options"} type={"bool | dict[str, bool] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"dispatchEvent"} type={"(event) -> bool"}>

Dispatch an event to the document.

<PySourceCode >

```python
def dispatchEvent(event: _Any) -> bool:
	"""Dispatch an event to the document."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"event"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>
<PyFunction name={"hasFocus"} type={"() -> bool"}>

Return True if the document has focus.

<PySourceCode >

```python
def hasFocus() -> bool:
	"""Return True if the document has focus."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"bool"} />

</PyFunction>
<PyFunction name={"getSelection"} type={"() -> _Any"}>

Return the current selection.

<PySourceCode >

```python
def getSelection() -> _Any:
	"""Return the current selection."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>
<PyFunction name={"importNode"} type={"(node, deep=False) -> _Element"}>

Import a node from another document.

<PySourceCode >

```python
def importNode(node: _Element, deep: bool = False, /) -> _Element:
	"""Import a node from another document."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"node"} type={"_Element"} value={null} />
<PyParameter name={"deep"} type={"bool"} value={"False"} />

</div>

<PyFunctionReturn type={"pulse.js._types.Element"} />

</PyFunction>
<PyFunction name={"adoptNode"} type={"(node) -> _Element"}>

Adopt a node from another document.

<PySourceCode >

```python
def adoptNode(node: _Element) -> _Element:
	"""Adopt a node from another document."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"node"} type={"_Element"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js._types.Element"} />

</PyFunction>
<PyFunction name={"exitFullscreen"} type={"() -> _Any"}>

Exit full-screen mode. Returns a Promise.

<PySourceCode >

```python
def exitFullscreen() -> _Any:
	"""Exit full-screen mode. Returns a Promise."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>

</Tab>

</Tabs>