---
title: "Promise"
---

JavaScript Promise - a thenable that represents an async operation.

Promise is both generic over its resolved type and implements Awaitable,
allowing it to be used with Python's async/await syntax which transpiles
to JavaScript async/await.

Example:
    @javascript
    async def fetch_data() -> str:
        response: Promise[Response] = fetch("/api/data")
        data = await response  # Awaits the promise
        return data.text()

## Functions

<PyFunction name={"__init__"} type={"(self, executor=None) -> None"}>

Create a Promise.

<PySourceCode >

```python
def __init__(
	self,
	executor: _Callable[
		[_Callable[[T_co], None], _Callable[[Exception], None]], None
	]
	| None = None,
	/,
) -> None:
	"""Create a Promise.

	Args:
	    executor: Optional function receiving (resolve, reject) callbacks.
	        If omitted, creates a pending promise (for use with Promise.resolve/reject).
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"executor"} type={"_Callable[[_Callable[[T_co], None], _Callable[[Exception], None]], None] | None"} value={"None"}>

Optional function receiving (resolve, reject) callbacks.
If omitted, creates a pending promise (for use with Promise.resolve/reject).

</PyParameter>

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

<PyFunction name={"then"} type={"(self, on_fulfilled=None, on_rejected=None) -> Promise[U]"}>

Attach callbacks to handle fulfillment and/or rejection.

<PySourceCode >

```python
def then(
	self,
	on_fulfilled: _Callable[[T_co], U | "Promise[U]"] | None = None,
	on_rejected: _Callable[[Exception], U | "Promise[U]"] | None = None,
	/,
) -> "Promise[U]":
	"""Attach callbacks to handle fulfillment and/or rejection."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"on_fulfilled"} type={"_Callable[[T_co], U | Promise[U]] | None"} value={"None"} />
<PyParameter name={"on_rejected"} type={"_Callable[[Exception], U | Promise[U]] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[pulse.js.promise.U]"} />

</PyFunction>

<PyFunction name={"catch"} type={"(self, on_rejected) -> Promise[T_co | U]"}>

Attach a rejection handler callback.

<PySourceCode >

```python
def catch(
	self, on_rejected: _Callable[[Exception], U | "Promise[U]"]
) -> "Promise[T_co | U]":
	"""Attach a rejection handler callback."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"on_rejected"} type={"_Callable[[Exception], U | Promise[U]]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[pulse.js.promise.T_co | pulse.js.promise.U]"} />

</PyFunction>

<PyFunction name={"finally_"} type={"(self, on_finally) -> Promise[T_co]"}>

Attach a handler that is called when the promise settles (fulfilled or rejected).

<PySourceCode >

```python
def finally_(self, on_finally: _Callable[[], None]) -> "Promise[T_co]":
	"""Attach a handler that is called when the promise settles (fulfilled or rejected)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"on_finally"} type={"_Callable[[], None]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[pulse.js.promise.T_co]"} />

</PyFunction>

<PyFunction name={"__await__"} type={"(self) -> _Generator[None, None, T_co]"}>

Support await syntax - transpiles to JavaScript await.

<PySourceCode >

```python
def __await__(self) -> _Generator[None, None, T_co]:
	"""Support await syntax - transpiles to JavaScript await."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"collections.abc.Generator[None, None, pulse.js.promise.T_co]"} />

</PyFunction>

<PyFunction name={"resolve"} type={"(value=None) -> Promise[U] | Promise[None]"}>

Create a Promise that resolves with the given value.

<PySourceCode >

```python
@staticmethod
def resolve(value: U | None = None, /) -> "Promise[U] | Promise[None]":
	"""Create a Promise that resolves with the given value."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"value"} type={"U | None"} value={"None"} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[pulse.js.promise.U] | pulse.js.promise.Promise[None]"} />

</PyFunction>

<PyFunction name={"reject"} type={"(reason) -> Promise[_Any]"}>

Create a Promise that rejects with the given reason.

<PySourceCode >

```python
@staticmethod
def reject(reason: Exception | str) -> "Promise[_Any]":
	"""Create a Promise that rejects with the given reason."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"reason"} type={"Exception | str"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[typing.Any]"} />

</PyFunction>

<PyFunction name={"all"} type={"(iterable) -> Promise[list[T]]"}>

Wait for all promises to resolve, or reject on first rejection.

Returns a promise that resolves to a list of all resolved values.

<PySourceCode >

```python
@staticmethod
def all(iterable: _Iterable["Promise[T]"]) -> "Promise[list[T]]":
	"""Wait for all promises to resolve, or reject on first rejection.

	Returns a promise that resolves to a list of all resolved values.
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"iterable"} type={"_Iterable[Promise[T]]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[list[pulse.js.promise.T]]"} />

</PyFunction>

<PyFunction name={"allSettled"} type={"(iterable) -> Promise[list[PromiseSettledResult[T]]]"}>

Wait for all promises to settle (resolve or reject).

Returns a promise that resolves to a list of result objects.

<PySourceCode >

```python
@staticmethod
def allSettled(
	iterable: _Iterable["Promise[T]"],
) -> "Promise[list[PromiseSettledResult[T]]]":
	"""Wait for all promises to settle (resolve or reject).

	Returns a promise that resolves to a list of result objects.
	"""
	...
```

</PySourceCode>

<div >

<PyParameter name={"iterable"} type={"_Iterable[Promise[T]]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[list[pulse.js.promise.PromiseSettledResult[pulse.js.promise.T]]]"} />

</PyFunction>

<PyFunction name={"any"} type={"(iterable) -> Promise[T]"}>

Return first fulfilled promise, or reject if all reject.

<PySourceCode >

```python
@staticmethod
def any(iterable: _Iterable["Promise[T]"]) -> "Promise[T]":
	"""Return first fulfilled promise, or reject if all reject."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"iterable"} type={"_Iterable[Promise[T]]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[pulse.js.promise.T]"} />

</PyFunction>

<PyFunction name={"race"} type={"(iterable) -> Promise[T]"}>

Return first settled promise (fulfilled or rejected).

<PySourceCode >

```python
@staticmethod
def race(iterable: _Iterable["Promise[T]"]) -> "Promise[T]":
	"""Return first settled promise (fulfilled or rejected)."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"iterable"} type={"_Iterable[Promise[T]]"} value={null} />

</div>

<PyFunctionReturn type={"pulse.js.promise.Promise[pulse.js.promise.T]"} />

</PyFunction>

<PyFunction name={"withResolvers"} type={"() -> PromiseWithResolvers[T]"}>

Create a promise with its resolve and reject functions exposed.

Returns an object with \{ promise, resolve, reject \}.
ES2024 feature.

<PySourceCode >

```python
@staticmethod
def withResolvers() -> "PromiseWithResolvers[T]":
	"""Create a promise with its resolve and reject functions exposed.

	Returns an object with { promise, resolve, reject }.
	ES2024 feature.
	"""
	...
```

</PySourceCode>

<PyFunctionReturn type={"pulse.js.promise.PromiseWithResolvers[pulse.js.promise.T]"} />

</PyFunction>