---
title: "window"
---

Browser window global object.

Usage:
    from pulse.js import window
    window.alert("Hello!")      # -> window.alert("Hello!")
    window.innerWidth           # -> window.innerWidth

<PyAttribute name={"innerWidth"} type={"int"} value={null} />

<PyAttribute name={"innerHeight"} type={"int"} value={null} />

<PyAttribute name={"outerWidth"} type={"int"} value={null} />

<PyAttribute name={"outerHeight"} type={"int"} value={null} />

<PyAttribute name={"scrollX"} type={"float"} value={null} />

<PyAttribute name={"scrollY"} type={"float"} value={null} />

<PyAttribute name={"pageXOffset"} type={"float"} value={null} />

<PyAttribute name={"pageYOffset"} type={"float"} value={null} />

<PyAttribute name={"devicePixelRatio"} type={"float"} value={null} />

<PyAttribute name={"location"} type={"_Any"} value={null} />

<PyAttribute name={"history"} type={"_Any"} value={null} />

<PyAttribute name={"navigator"} type={"_Any"} value={null} />

<PyAttribute name={"document"} type={"_Any"} value={null} />

<PyAttribute name={"localStorage"} type={"_Any"} value={null} />

<PyAttribute name={"sessionStorage"} type={"_Any"} value={null} />

<PyAttribute name={"performance"} type={"_Any"} value={null} />

<Tabs items={["Functions"]}>

<Tab value={"Functions"}>

<PyFunction name={"alert"} type={"(message='') -> None"}>

Display an alert dialog with the given message.

<PySourceCode >

```python
def alert(message: str = "", /) -> None:
	"""Display an alert dialog with the given message."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"message"} type={"str"} value={"''"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"confirm"} type={"(message='') -> bool"}>

Display a confirmation dialog. Returns True if user clicks OK.

<PySourceCode >

```python
def confirm(message: str = "", /) -> bool:
	"""Display a confirmation dialog. Returns True if user clicks OK."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"message"} type={"str"} value={"''"} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>
<PyFunction name={"prompt"} type={"(message='', default='') -> str | None"}>

Display a prompt dialog. Returns input or None if cancelled.

<PySourceCode >

```python
def prompt(message: str = "", default: str = "", /) -> str | None:
	"""Display a prompt dialog. Returns input or None if cancelled."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"message"} type={"str"} value={"''"} />
<PyParameter name={"default"} type={"str"} value={"''"} />

</div>

<PyFunctionReturn type={"str | None"} />

</PyFunction>
<PyFunction name={"scrollTo"} type={"(x, y=None) -> None"}>

Scroll to the given position.

<PySourceCode >

```python
def scrollTo(x: float | dict[str, float], y: float | None = None, /) -> None:
	"""Scroll to the given position."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"x"} type={"float | dict[str, float]"} value={null} />
<PyParameter name={"y"} type={"float | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"scrollBy"} type={"(x, y=None) -> None"}>

Scroll by the given amount.

<PySourceCode >

```python
def scrollBy(x: float | dict[str, float], y: float | None = None, /) -> None:
	"""Scroll by the given amount."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"x"} type={"float | dict[str, float]"} value={null} />
<PyParameter name={"y"} type={"float | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"scroll"} type={"(x, y=None) -> None"}>

Alias for scrollTo.

<PySourceCode >

```python
def scroll(x: float | dict[str, float], y: float | None = None, /) -> None:
	"""Alias for scrollTo."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"x"} type={"float | dict[str, float]"} value={null} />
<PyParameter name={"y"} type={"float | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"getSelection"} type={"() -> _Selection | None"}>

Return the current text selection.

<PySourceCode >

```python
def getSelection() -> _Selection | None:
	"""Return the current text selection."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"pulse.js._types.Selection | None"} />

</PyFunction>
<PyFunction name={"getComputedStyle"} type={"(element, pseudoElt=None) -> _Any"}>

Return the computed style of an element.

<PySourceCode >

```python
def getComputedStyle(element: _Element, pseudoElt: str | None = None, /) -> _Any:
	"""Return the computed style of an element."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"element"} type={"_Element"} value={null} />
<PyParameter name={"pseudoElt"} type={"str | None"} value={"None"} />

</div>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>
<PyFunction name={"focus"} type={"() -> None"}>

Give focus to the window.

<PySourceCode >

```python
def focus() -> None:
	"""Give focus to the window."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"blur"} type={"() -> None"}>

Remove focus from the window.

<PySourceCode >

```python
def blur() -> None:
	"""Remove focus from the window."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"open"} type={"(url='', target='_blank', features='') -> _Any | None"}>

Open a new window. Returns the new window object or None.

<PySourceCode >

```python
def open(
	url: str = "",
	target: str = "_blank",
	features: str = "",
	/,
) -> _Any | None:
	"""Open a new window. Returns the new window object or None."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"url"} type={"str"} value={"''"} />
<PyParameter name={"target"} type={"str"} value={"'_blank'"} />
<PyParameter name={"features"} type={"str"} value={"''"} />

</div>

<PyFunctionReturn type={"typing.Any | None"} />

</PyFunction>
<PyFunction name={"close"} type={"() -> None"}>

Close the window (only works for windows opened by script).

<PySourceCode >

```python
def close() -> None:
	"""Close the window (only works for windows opened by script)."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"setTimeout"} type={"(handler, timeout=0, *args) -> int"}>

Schedule a function to run after a delay. Returns timer ID.

<PySourceCode >

```python
def setTimeout(handler: _Callable[..., None], timeout: int = 0, /, *args: _Any) -> int:
	"""Schedule a function to run after a delay. Returns timer ID."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"handler"} type={"_Callable[..., None]"} value={null} />
<PyParameter name={"timeout"} type={"int"} value={"0"} />
<PyParameter name={"args"} type={"_Any"} value={"()"} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>
<PyFunction name={"clearTimeout"} type={"(timeoutId) -> None"}>

Cancel a timeout scheduled with setTimeout.

<PySourceCode >

```python
def clearTimeout(timeoutId: int) -> None:
	"""Cancel a timeout scheduled with setTimeout."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"timeoutId"} type={"int"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"setInterval"} type={"(handler, timeout=0, *args) -> int"}>

Schedule a function to run repeatedly. Returns timer ID.

<PySourceCode >

```python
def setInterval(handler: _Callable[..., None], timeout: int = 0, /, *args: _Any) -> int:
	"""Schedule a function to run repeatedly. Returns timer ID."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"handler"} type={"_Callable[..., None]"} value={null} />
<PyParameter name={"timeout"} type={"int"} value={"0"} />
<PyParameter name={"args"} type={"_Any"} value={"()"} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>
<PyFunction name={"clearInterval"} type={"(intervalId) -> None"}>

Cancel an interval scheduled with setInterval.

<PySourceCode >

```python
def clearInterval(intervalId: int) -> None:
	"""Cancel an interval scheduled with setInterval."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"intervalId"} type={"int"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"requestAnimationFrame"} type={"(callback) -> int"}>

Request a callback before the next repaint. Returns request ID.

<PySourceCode >

```python
def requestAnimationFrame(callback: _Callable[[float], None]) -> int:
	"""Request a callback before the next repaint. Returns request ID."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"callback"} type={"_Callable[[float], None]"} value={null} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>
<PyFunction name={"cancelAnimationFrame"} type={"(requestId) -> None"}>

Cancel an animation frame request.

<PySourceCode >

```python
def cancelAnimationFrame(requestId: int) -> None:
	"""Cancel an animation frame request."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"requestId"} type={"int"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"addEventListener"} type={"(type, listener, options=None) -> None"}>

Add an event listener to the window.

<PySourceCode >

```python
def addEventListener(
	type: str,
	listener: _Callable[..., None],
	options: bool | dict[str, bool] | None = None,
	/,
) -> None:
	"""Add an event listener to the window."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"type"} type={"str"} value={null} />
<PyParameter name={"listener"} type={"_Callable[..., None]"} value={null} />
<PyParameter name={"options"} type={"bool | dict[str, bool] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"removeEventListener"} type={"(type, listener, options=None) -> None"}>

Remove an event listener from the window.

<PySourceCode >

```python
def removeEventListener(
	type: str,
	listener: _Callable[..., None],
	options: bool | dict[str, bool] | None = None,
	/,
) -> None:
	"""Remove an event listener from the window."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"type"} type={"str"} value={null} />
<PyParameter name={"listener"} type={"_Callable[..., None]"} value={null} />
<PyParameter name={"options"} type={"bool | dict[str, bool] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"dispatchEvent"} type={"(event) -> bool"}>

Dispatch an event to the window.

<PySourceCode >

```python
def dispatchEvent(event: _Any) -> bool:
	"""Dispatch an event to the window."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"event"} type={"_Any"} value={null} />

</div>

<PyFunctionReturn type={"bool"} />

</PyFunction>
<PyFunction name={"atob"} type={"(encoded) -> str"}>

Decode a base64 encoded string.

<PySourceCode >

```python
def atob(encoded: str) -> str:
	"""Decode a base64 encoded string."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"encoded"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"str"} />

</PyFunction>
<PyFunction name={"btoa"} type={"(data) -> str"}>

Encode a string as base64.

<PySourceCode >

```python
def btoa(data: str) -> str:
	"""Encode a string as base64."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"data"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"str"} />

</PyFunction>
<PyFunction name={"matchMedia"} type={"(query) -> _Any"}>

Return a MediaQueryList for the given media query.

<PySourceCode >

```python
def matchMedia(query: str) -> _Any:
	"""Return a MediaQueryList for the given media query."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"query"} type={"str"} value={null} />

</div>

<PyFunctionReturn type={"typing.Any"} />

</PyFunction>
<PyFunction name={"print_"} type={"() -> None"}>

Open the print dialog.

<PySourceCode >

```python
def print_() -> None:
	"""Open the print dialog."""
	...
```

</PySourceCode>

<PyFunctionReturn type={"None"} />

</PyFunction>
<PyFunction name={"postMessage"} type={"(message, targetOrigin, transfer=None) -> None"}>

Post a message to another window.

<PySourceCode >

```python
def postMessage(
	message: _Any, targetOrigin: str, transfer: list[_Any] | None = None, /
) -> None:
	"""Post a message to another window."""
	...
```

</PySourceCode>

<div >

<PyParameter name={"message"} type={"_Any"} value={null} />
<PyParameter name={"targetOrigin"} type={"str"} value={null} />
<PyParameter name={"transfer"} type={"list[_Any] | None"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

</Tab>

</Tabs>