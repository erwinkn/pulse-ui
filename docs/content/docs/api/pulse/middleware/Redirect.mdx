---
title: "Redirect"
---

Redirect response. Causes navigation to the specified path.

## Attributes

<PyAttribute name={"path"} type={"str"} value={"path"}>

The path to redirect to.

</PyAttribute>

## Functions

<PyFunction name={"__init__"} type={"(self, path) -> None"}>

Initialize a redirect response.

<PySourceCode >

```python
def __init__(self, path: str) -> None:
	"""Initialize a redirect response.

	Args:
		path: The path to redirect to.
	"""
	self.path = path
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"path"} type={"str"} value={undefined}>

The path to redirect to.

</PyParameter>

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>