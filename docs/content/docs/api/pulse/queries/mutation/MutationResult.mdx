---
title: "MutationResult"
---

Result object for mutations providing reactive state and execution.

MutationResult wraps an async mutation function and provides reactive
access to its execution state. It is callable to execute the mutation.

## Attributes

<PyAttribute name={"_data"} type={"Signal[T | None]"} value={"Signal(None, name='mutation.data')"} />

<PyAttribute name={"_is_running"} type={"Signal[bool]"} value={"Signal(False, name='mutation.is_running')"} />

<PyAttribute name={"_error"} type={"Signal[Exception | None]"} value={"Signal(None, name='mutation.error')"} />

<PyAttribute name={"_fn"} type={"Callable[P, Awaitable[T]]"} value={"fn"} />

<PyAttribute name={"_on_success"} type={"Callable[[T], Any] | None"} value={"on_success"} />

<PyAttribute name={"_on_error"} type={"Callable[[Exception], Any] | None"} value={"on_error"} />

<PyAttribute name={"data"} type={"T | None"} value={null}>

The last successful mutation result, or None.

</PyAttribute>

<PyAttribute name={"is_running"} type={"bool"} value={null}>

Whether the mutation is currently executing.

</PyAttribute>

<PyAttribute name={"error"} type={"Exception | None"} value={null}>

The last error encountered, or None.

</PyAttribute>

## Functions

<PyFunction name={"__init__"} type={"(self, fn, on_success=None, on_error=None)"}>

Initialize the mutation result.

<PySourceCode >

```python
def __init__(
	self,
	fn: Callable[P, Awaitable[T]],
	on_success: Callable[[T], Any] | None = None,
	on_error: Callable[[Exception], Any] | None = None,
):
	"""Initialize the mutation result.

	Args:
		fn: The bound async function to execute.
		on_success: Optional callback invoked on successful completion.
		on_error: Optional callback invoked on error.
	"""
	self._data = Signal(None, name="mutation.data")
	self._is_running = Signal(False, name="mutation.is_running")
	self._error = Signal(None, name="mutation.error")
	self._fn = fn
	self._on_success = on_success
	self._on_error = on_error
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"fn"} type={"Callable[P, Awaitable[T]]"} value={undefined}>

The bound async function to execute.

</PyParameter>
<PyParameter name={"on_success"} type={"Callable[[T], Any] | None"} value={"None"}>

Optional callback invoked on successful completion.

</PyParameter>
<PyParameter name={"on_error"} type={"Callable[[Exception], Any] | None"} value={"None"}>

Optional callback invoked on error.

</PyParameter>

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"__call__"} type={"(self, *args, **kwargs) -> T"}>

<PySourceCode >

```python
async def __call__(self, *args: P.args, **kwargs: P.kwargs) -> T:
	self._is_running.write(True)
	self._error.write(None)
	try:
		mutation_result = await self._fn(*args, **kwargs)
		self._data.write(mutation_result)
		if self._on_success:
			await maybe_await(call_flexible(self._on_success, mutation_result))
		return mutation_result
	except Exception as e:
		self._error.write(e)
		if self._on_error:
			await maybe_await(call_flexible(self._on_error, e))
		raise e
	finally:
		self._is_running.write(False)
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"args"} type={"P.args"} value={"()"} />
<PyParameter name={"kwargs"} type={"P.kwargs"} value={"{}"} />

</div>

<PyFunctionReturn type={"pulse.queries.mutation.T"} />

</PyFunction>