---
title: "Ternary"
---

JS ternary expression: cond ? a : b

## Attributes

<PyAttribute name={"cond"} type={"Expr"} value={null} />

<PyAttribute name={"then"} type={"Expr"} value={null} />

<PyAttribute name={"else_"} type={"Expr"} value={null} />

## Functions

<PyFunction name={"precedence"} type={"(self) -> int"}>

<PySourceCode >

```python
@override
def precedence(self) -> int:
	return _PRECEDENCE["?:"]
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"int"} />

</PyFunction>

<PyFunction name={"emit"} type={"(self, out) -> None"}>

<PySourceCode >

```python
@override
def emit(self, out: list[str]) -> None:
	self.cond.emit(out)
	out.append(" ? ")
	self.then.emit(out)
	out.append(" : ")
	self.else_.emit(out)
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"out"} type={"list[str]"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>

<PyFunction name={"render"} type={"(self) -> VDOMNode"}>

<PySourceCode >

```python
@override
def render(self) -> VDOMNode:
	return {
		"t": "ternary",
		"cond": self.cond.render(),
		"then": self.then.render(),
		"else_": self.else_.render(),
	}
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"pulse.transpiler.vdom.VDOMNode"} />

</PyFunction>

<PyFunction name={"__init__"} type={"(self, cond, then, else_) -> None"}>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"cond"} type={"Expr"} value={null} />
<PyParameter name={"then"} type={"Expr"} value={null} />
<PyParameter name={"else_"} type={"Expr"} value={null} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>