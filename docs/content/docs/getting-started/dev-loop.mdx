---
title: "Dev Loop"
---

Pulse runs a Python server + React client. `pulse run` watches both.

```bash
uv run pulse run path/to/app.py
```

Use `q` to stop. Use `--server-only` / `--web-only` if you split dev servers.
