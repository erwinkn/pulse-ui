---
title: "Forms"
description: "Form helpers and types"
---


Exports:
`Form`, `FormData`, `FormValue`, `ManualForm`, `UploadFile`

Notes:
- `Form` is server-registered and requires a `key`
- `ManualForm` exposes `props()` for manual form wiring
