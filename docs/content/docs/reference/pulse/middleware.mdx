---
title: "Middleware"
description: "Pulse middleware types"
---


Exports:
`ConnectResponse`, `Deny`, `LatencyMiddleware`, `MiddlewareStack`, `NotFound`, `Ok`, `PrerenderResponse`, `PulseMiddleware`, `Redirect`, `RoutePrerenderResponse`, `stack`

Notes:
- Use `stack(...)` to compose middleware
- `LatencyMiddleware` is dev-only
