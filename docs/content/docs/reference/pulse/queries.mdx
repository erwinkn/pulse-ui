---
title: "Queries"
description: "Query, mutation, and infinite query APIs"
---


Decorators:
`query`, `mutation`, `infinite_query`

Client + types:
`QueryClient`, `QueryFilter`, `queries`, `ActionError`, `ActionResult`, `ActionSuccess`, `QueryKey`, `QueryStatus`, `QueryResult`

Notes:
- Queries are defined on `State` methods
- Keyed queries use `.key` decorator
