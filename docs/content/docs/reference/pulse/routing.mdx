---
title: "Routing"
description: "Routes, layouts, and client routing components"
---


Exports:
`Layout`, `Route`, `RouteInfo`, `Link`, `Outlet`

Notes:
- `Route` paths are relative
- Use `Link` and `navigate()` for client navigation
