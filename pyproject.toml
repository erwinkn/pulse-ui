[project]
name = "pulse-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pulse-framework", "pulse-lucide", "pulse-recharts"]


[dependency-groups]
dev = [
    "mypy>=1.17.0",
    "pytest>=8.3.4",
    "pytest-asyncio>=1.1.0",
]

[tool.uv.sources]
pulse-framework = { workspace = true }
pulse-lucide = { workspace = true }
pulse-recharts = { workspace = true }


[tool.uv.workspace]
members = ["packages/*"]
exclude = ["packages/pulse-ui-client"]
